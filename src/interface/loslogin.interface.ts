interface loslogin {
  map(
    arg0: (losLogin: any) => import("react/jsx-runtime").JSX.Element
  ): React.ReactNode;
  rcc: string;
  losnane: string;
  lospassw: string;
  ipin: string;
  seqlose: string;
  alert: string;
}

interface checkboxsLoslogin {
  rcc: string;
}
